//import { defineConfig } from 'astro/config'; export default defineConfig({});

import { defineConfig } from 'astro/config'
export default defineConfig({site: 'https://twitchingastronaut.github.io', base: '/resume.new',})